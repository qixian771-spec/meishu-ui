# 13-01 — Precomposed builders + primitives

## Deliverables

- `src/glass/precomposed/buildLayers.ts` — pure specs from ThemeTokens + depth
- React: `PrecomposedAtmosphere`, `PrecomposedShell`, `PrecomposedPane`, `PrecomposedInset`
- CSS: `precomposed.css` (no backdrop-filter)
- Export from `src/glass/index.ts`
- Tests: buildLayers + component smoke

## Must

- `resolveBlurForDepth` for nest budget
- Premultiplied wash fills from token RGB + alpha
- No hardcoded brand hex in precomposed sources
